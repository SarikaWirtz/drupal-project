name: Hero Slider
description: Splide.js hero slider using slide title, image and short text.
props:
  options:
    type: string
    required: false
slots:
  default:
    title: Optional overlay slot
  images:
      title: Media
      type: array
      required: true
